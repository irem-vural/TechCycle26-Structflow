/**
 * User-approved project/Excel benchmark values shared by the engineering
 * engine. These are project inputs/assumptions, not values prescribed by ACI.
 */
export const PROJECT_CONCRETE_UNIT_WEIGHT_KN_M3 = 23.5 as const;
